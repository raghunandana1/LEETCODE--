well welcome dawg!
if u dont know what a palindrome is lemme break it dowon for you,
so basically palindrome is a set of characters in the form of strings or numbers that read the same as backwards as forward.
for example, in strings-- racecar---if we read the string in reverse we will be getting the same string/
